{
  "installCommand": "cd raskon-appart && npm install",
  "buildCommand": "cd raskon-appart && npm run build",
  "outputDirectory": "raskon-appart/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}